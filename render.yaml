services:
  - type: web
    name: nonprofit-coach
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd nonprofit_coach && python app.py
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: SECRET_KEY
        generateValue: true
